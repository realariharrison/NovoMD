# Server runtime dependencies for the NovoMD REST service (Docker / uvicorn).
# For the library alone, prefer: pip install novomd
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
slowapi==0.1.9

# Molecular property core
numpy>=1.24.3,<2.0.0
scipy>=1.11.4
rdkit>=2023.3.1

# OpenBabel is optional (extra conversion features); install via conda or
# system package manager if needed.
